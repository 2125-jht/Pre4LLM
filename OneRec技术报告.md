# OneRec：快手统一生成式推荐系统技术报告

> 参考文献：
> - OneRec Technical Report: https://arxiv.org/abs/2506.13695
> - OneRec论文: https://arxiv.org/abs/2502.18965
> - OneRec-V2: https://arxiv.org/abs/2508.20900

---

## 1. 研究背景与动机

### 1.1 传统级联推荐系统的问题

传统推荐系统通常采用多阶段级联架构（召回 → 粗排 → 精排 → 重排），这种架构存在三大核心问题：

| 问题 | 描述 |
|------|------|
| **计算碎片化** | 以快手为例，服务期间超过50%的资源被分配给通信和存储，而非高精度计算。传统排序模型的训练和推理MFU分别仅为4.6%和11.2%，远低于大语言模型（约40%）。高QPS（>400k）和低延迟（<500ms）的需求进一步限制了模型规模。 |
| **目标冲突** | 一方面，用户/创作者/平台之间存在数百个互相竞争的目标，在不同阶段介入会逐渐破坏系统一致性；另一方面，不同阶段的模型结构和规模差异导致跨阶段建模冲突。 |
| **技术代差** | 级联架构难以有效吸纳LLM和VLM领域的关键突破技术（如Scaling Laws、强化学习等），导致推荐系统与主流AI技术之间存在越来越大的差距。 |

### 1.2 OneRec的核心思路

OneRec通过**端到端生成式方法**重塑推荐系统，将传统"召回+排序"级联流程替换为**单阶段生成式模型**，直接自回归地生成用户可能感兴趣的视频语义ID。

核心特点：
- **端到端优化**：直接优化最终目标，避免级联系统中各阶段目标不一致的问题
- **计算高效**：统一架构减少了通信和存储开销，使运营成本降至传统推荐管道的约10.6%
- **统一生成**：通过单个编码器-解码器架构，编码用户历史行为序列，逐token解码推荐视频

---

## 2. 整体架构

OneRec采用**编码器-解码器（Encoder-Decoder）**架构，工作流程如下：

```
┌─────────────────────────────────────────────────────────────────────┐
│                         OneRec 架构                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   输入视频 → [Tokenizer] → 语义ID (Semantic IDs)                    │
│                              ↑                                       │
│                              │ 目标                                   │
│   用户特征 → [Encoder] ──────┐                                       │
│                ↓              │                                       │
│           上下文表示    [Decoder] ← BOS + 目标语义ID                 │
│                ↓              │                                       │
│         [Reward System] ←── 生成结果                                 │
│                ↓                                                     │
│           强化学习对齐                                               │
│                                                                      │
│   推理：模型生成语义ID → 映射回视频ID → 推荐                         │
└─────────────────────────────────────────────────────────────────────┘
```

主要组件：
1. **Tokenizer**：将视频转换为层次化语义ID
2. **Encoder**：编码用户多尺度行为序列
3. **Decoder**：自回归解码目标视频的语义ID
4. **Reward System**：通过强化学习对齐用户偏好和业务目标

---

## 3. Tokenizer：视频语义ID生成

### 3.1 设计目标

快手拥有十亿级别且不断增长的视频库，直接生成原子视频ID不可行。Tokenizer将视频转换为**粗到细（coarse-to-fine）的语义ID**，使用固定且精简的词表，实现相似视频间的知识迁移和对新视频的泛化。

### 3.2 对齐协同感知的多模态表示

区别于仅基于上下文特征生成语义ID的先前方案，OneRec**整合协同信号与多模态特征**：

**多模态表示提取：**
- 对每个视频提取以下多模态输入：字幕（caption）、标签（tag）、ASR（语音转文字）、OCR（图像转文字）、封面图、5张均匀采样的帧
- 使用 **miniCPM-V-8B** 处理这些输入，生成1280个token向量，每个维度为512
- 通过 **QFormer** 将1280个token压缩为4个可学习的查询token

```
视频多模态输入 → miniCPM-V-8B → 1280 tokens × 512d
                                      ↓
                                  QFormer (4层)
                                      ↓
                                   4 tokens × 512d (压缩表示)
```

**高质量Item-Pair构建：**
- **User-to-Item检索**：对每个用户，将正样本目标视频与其最近历史正点击中协同相似度最高的视频配对
- **Item-to-Item检索**：使用Swing相似度等指标，将高相似度的视频配对

**双目标训练：**
- **Item-to-Item对比损失**：对齐协同相似视频对的表示，捕捉行为模式
- **字幕生成损失**：使用LLaMA3解码器对视频字幕进行Next-Token预测，防止模型产生幻觉，保持内容理解能力

### 3.3 RQ-Kmeans语义ID生成

使用 **RQ-Kmeans（残差量化K-means）** 进行tokenization，生成层次化的语义ID：

```
输入：视频的多模态压缩表示 M̃ ∈ R^(4×512)
输出：3层粗到细的语义ID {s^1, s^2, s^3}

第1层：R^(1) = {M̃_i}                     # 初始残差
       C^(1) = K-means(R^(1), N_t)        # N_t = 词表大小
       s_i^1 = argmin_k ||R_i^(1) - c_k^(1)||
       R_i^(2) = R_i^(1) - c_{s_i^1}^(1)  # 更新残差

第2层：重复上述过程...
第3层：重复上述过程...
```

RQ-Kmeans相比常用的RQ-VAE具有更好的重建质量、码本利用率和平衡性。每个视频最终被表示为3个从粗到细的语义标识符。

**关键优势**：
- 仅依赖视频原始内容进行编码，**无需线上样本积累**
- 线上系统数月不更新，也能编码最新上传的短视频
- 完成新视频ID编码后即可对其进行推荐

---

## 4. Encoder：用户兴趣建模

### 4.1 多尺度特征工程

Encoder通过**四条专门的嵌入路径**处理用户行为数据，每条路径捕捉不同尺度的用户交互模式：

| 路径 | 序列长度 | 功能 | 描述 |
|------|---------|------|------|
| **用户静态路径** | 1 | 用户画像 | uid、年龄、性别等静态特征 → Dense层 → h_u ∈ R^(1×d_model) |
| **短期路径** | L_s=20 | 即时偏好 | 最近20个交互（视频ID/语义ID、作者ID、标签、时间戳、播放时长、视频时长、交互标签等） |
| **正反馈路径** | L_p=256 | 高互动偏好 | 256个高参与度交互序列 |
| **终身路径** | N_q=128 | 长期兴趣 | 通过层次化K-means压缩最长10万个视频的历史序列 |

**终身路径的两阶段层次化压缩策略：**

1. **行为压缩**：基于多模态内容表示，对每个用户的交互序列进行层次化K-means聚类
   - 每步聚类数设为 ⌊|D|/3⌋，其中|D|为当前数据中的视频数
   - 聚类终止条件：当前簇中的视频数不超过预设阈值M
   - 选择每个簇中心最近的视频作为簇代表

2. **特征聚合**：
   - 离散特征（vid、aid、label）：直接继承代表视频的特征
   - 连续特征（tag、ts、playtime、duration）：计算簇内所有视频的平均值

3. **QFormer压缩**：将2000个聚类代表通过2层QFormer交叉注意力压缩为128个查询token

### 4.2 编码器架构

```
四条路径输出拼接：
  z^(1) = [h_u; h_s; h_p; h_l] + e_pos

  总序列长度 = 1 + 20 + 256 + 128 = 405

通过 L_enc 层Transformer编码器：
  z^(i+1) = z^(i) + SelfAttn(RMSNorm(z^(i)))
  z^(i+1) = z^(i+1) + FFN(RMSNorm(z^(i+1)))

输出：z_enc ∈ R^(405×d_model)
```

采用**全可见的自注意力机制**（fully visible self-attention）和RMSNorm，提供整体的多尺度用户行为表示。

---

## 5. Decoder：自回归推荐生成

### 5.1 逐点生成范式

对于每个目标视频m，解码器输入序列由可学习的BOS token和视频的语义ID拼接而成：

```
S_m = {s_[BOS], s_m^1, s_m^2, ..., s_m^Lt}  (Lt=3)
d_m^(0) = Emb_lookup(S_m)
```

通过 L_dec 层Transformer解码器，每层依次执行：

```
d^(i+1) = d^(i) + CausalSelfAttn(d^(i))           # 因果自注意力
d^(i+1) = d^(i+1) + CrossAttn(d^(i+1), z_enc, z_enc)  # 交叉注意力
d^(i+1) = d^(i+1) + MoE(RMSNorm(d^(i+1)))         # MoE前馈网络
```

### 5.2 稀疏MoE扩展

使用 **稀疏混合专家（Mixture of Experts, MoE）** 扩展模型容量，不按比例增加计算FLOPs：

```
MoE(x) = Σ_{j=1}^k Gate_j(x) · Expert_j(x)
```

- 采用 **top-k路由策略** 从N个专家中选择k个
- 使用 **无辅助损失的负载均衡策略**（loss-free load balancing），确保专家利用率均衡且不引入干扰梯度
- MoE变体使用 **SwiGLU FFN** 作为专家，隐藏维度计算为 (2/3) × 4 × d_model

### 5.3 训练损失

模型使用**交叉熵损失**进行下一个token预测：

```
L_NTP = -Σ_{j=0}^{Lt-1} log P(s_m^{j+1} | [s_[BOS], s_m^1, ..., s_m^j])
```

目标视频m的3个语义ID被逐个自回归地预测。

---

## 6. Reward System：偏好对齐与强化学习

预训练模型仅拟合了由传统推荐系统曝光的物品分布，无法突破传统推荐的天花板。为此，OneRec引入了基于**Reward System的偏好对齐**，使用在线强化学习在生成空间中训练模型。

Reward System由三部分组成：

### 6.1 用户偏好Reward（P-Score）

**问题**：传统方法对ctr、点赞、评论、观看时长等多个目标分别预测后手工加权融合，缺乏准确性和个性化，且往往导致目标间的优化冲突。

**解决方案**：使用神经网络学习**个性化的融合得分P-Score（Preference Score）**。

架构设计：
- 底层基于 **SIM（Search-based Interest Model）** 架构
- 包含**多个独立塔（tower）**，每个塔学习一个特定目标（ctr、lvtr、ltr、vtr等）
- 训练时各塔使用对应目标标签计算BCE损失作为辅助任务
- 各塔的隐状态 + 用户表示 + 物品表示 → 输入最终MLP → 单塔输出P-Score

关键公式：
```
L_P-Score = Σ_{xtr ∈ S_o} w_xtr · L_P-Score^{xtr}
L_P-Score^{xtr} = -(y_xtr · log(p) + (1-y_xtr) · log(1-p))
S_o = {ctr, lvtr, ltr, vtr, ...}
```

通过调整w_xtr使P-Score偏向不同目标，实现各目标AUC的综合提升。这种方法允许模型接收特定用户信息并适当调整偏好得分，更有可能实现**帕累托优化**。

### 6.2 格式Reward（Format Reward）

**问题**：引入RL后，模型可能生成无法映射到实际视频ID的非法语义ID序列。这是由于负优势（negative advantage）导致的**挤压效应（squeezing effect）**——负样本将概率质量压缩到模型认为最优的输出上，使合法token的概率被挤压到与非法token相当的水平。

**解决方案**：在RL中加入格式奖励，鼓励合法生成：
- 从G个生成样本中随机选择K个进行合法性强化学习
- **合法样本**：设置advantage为1
- **非法样本**：直接丢弃，避免挤压效应

```
A_i = { 1  if o_i ∈ I_legal
      { 0  if o_i ∉ I_legal
```

### 6.3 工业场景Reward

工业推荐系统除了用户偏好外，还需考虑视频社区生态、商业化需求、冷启动/长尾视频分发等。传统系统在不同阶段分别处理这些问题，容易导致各阶段不一致。

OneRec的做法：**将优化目标纳入Reward System，通过强化学习进行端到端优化**。例如要控制"营销感"内容占比，只需界定"营销感"并设定相应Reward即可。系统能在天级完成响应，所需样本量约为传统方法的1/1000。

### 6.4 ECPO：Early Clipped GRPO

OneRec使用 **ECPO（Early Clipped GRPO）** 进行策略优化，是对GRPO的改进：

```
J_ECPO(θ) = E_{u~P(U), {o_i}_{i=1}^G ~ π_θ_old}[
    (1/G) Σ_{i=1}^G min(
        (π_θ(o_i|u) / π_θ_old'(o_i|u)) · A_i,
        clip(π_θ(o_i|u) / π_θ_old'(o_i|u), 1-ε, 1+ε) · A_i
    )
]

A_i = (r_i - mean({r_1,...,r_G})) / std({r_1,...,r_G})    # 归一化优势

π_θ_old'(o_i|u) = max(sg(π_θ(o_i|u))/(1+ε+δ), π_θ_old(o_i|u))  # 早期裁剪
```

**改进点**：原始GRPO允许负优势下存在较大的策略比率（π_θ/π_θ_old），容易导致梯度爆炸。ECPO**预先裁剪**具有大比率的策略，确保训练稳定，同时仍允许负优势生效。在OneRec中设置δ=0.1。同时移除了KL散度损失，因为RL和SFT是联合训练的，SFT损失已经确保模型稳定。

---

## 7. 训练框架

### 7.1 训练基础设施

| 配置 | 详情 |
|------|------|
| 计算 | 90台服务器，每台8张旗舰GPU + 2 CPU，NVLink 400Gbps |
| 网络 | 节点内：NVLink；节点间：400Gbps RDMA（训练流量）+ 100Gbps TCP（数据预取） |
| 存储 | 每台服务器4块NVMe SSD，embedding和dense参数存储在HDFS |

**训练加速优化：**

1. **Embedding加速**：使用快手自研SKAI框架的GPU参数服务器，利用跨GPU统一embedding表、GPU缓存和预取管道
2. **训练并行**：数据并行 + ZERO1 + 梯度累积。选择ZERO1因为dense参数可加载到单GPU上
3. **混合精度**：部分MLP网络使用BFloat16
4. **编译优化**：对注意力网络应用编译优化

### 7.2 预训练

**训练数据：**
- 每个训练样本包含一个目标视频，被token化为3个语义标识符
- 每天处理约180亿样本，产生540亿token
- OneRec-0.935B模型在约1000亿样本上收敛（共3000亿token曝光）

**模型规格：**

| 模型 | 层数 (Enc+Dec) | 隐层维度 | FFN隐层维度 | 注意力头数 | 专家数 (总/激活) | MoE位置 |
|------|---------------|---------|------------|-----------|-----------------|---------|
| OneRec-0.015B (Dense) | 4 | 128 | 256 | 4 | N/A | N/A |
| OneRec-0.121B (Dense) | 8 | 1024 | 2048 | 8 | N/A | N/A |
| OneRec-0.935B (MoE) | 8 | 1024 | 2048 | 8 | 24/2 | Decoder |
| OneRec-2.633B (MoE) | 24 | 1024 | 2048 | 8 | 24/4 | Enc & Dec |

### 7.3 后训练（Post-training）

后训练阶段使用实时数据流进行在线训练，**同时执行RSFT和RL**：

**RSFT（Reject Sampling Fine-Tuning）：**
- 过滤掉播放时长排名后50%的曝光会话
- 训练损失与预训练相同（L_NTP），但学习率降低
  - 稀疏参数：1×10^-4
  - 稠密参数：8×10^-5

**RL（Reinforcement Learning）：**
- 从RSFT数据中随机选择1%的用户生成RL样本
- 使用外部推理服务解耦RL样本生成与训练过程
- 训练时1%的用户访问外部服务生成512个物品，从Reward Model获取奖励
- 训练任务每1000步通过消息队列（MQ）将更新参数发送给外部推理服务

---

## 8. 推理流程

```
1. 用户请求 → Encoder编码用户多尺度行为序列 → 上下文表示
                    ↓
2. Decoder自回归生成语义ID序列
   - 从BOS token开始
   - 逐层预测粗→中→细的语义ID
   - 可选：使用beam search等解码策略
                    ↓
3. 语义ID → 视频ID映射
   - 将生成的语义ID序列映射回实际视频ID
   - 如果序列无对应视频，则为非法生成
                    ↓
4. [可选] Reward-based选择步骤
   - 将生成的视频送入Reward Model
   - 根据P-Score进一步筛选/重排
                    ↓
5. 返回最终推荐列表
```

---

## 9. 关键设计亮点总结

| 维度 | OneRec的做法 | 传统系统的做法 |
|------|-------------|---------------|
| 架构 | 单阶段端到端生成式 | 多阶段级联（召回→粗排→精排→重排） |
| 预测目标 | 自回归生成语义ID序列 | 对每个候选打分排序 |
| 用户建模 | 多尺度行为序列（4条路径） | 通常单一序列 |
| 长序列处理 | 层次化K-means + QFormer压缩 | 截断或简单Pooling |
| 多目标优化 | P-Score神经网络自动融合 | 手工加权融合 |
| 业务适配 | 定义Reward即可，天级响应 | 各阶段分别修改，易臃肿 |
| 模型扩展 | 稀疏MoE线性扩展容量 | Dense模型计算随参数线性增长 |

---

## 10. 核心创新点

1. **首个工业级端到端生成式推荐模型**：将检索和排序统一为单一生成模型，在快手主站部署25%流量

2. **协同感知的多模态Tokenizer**：整合协同信号与多模态特征，使用RQ-Kmeans生成高质量层次化语义ID

3. **多尺度用户兴趣编码**：通过用户静态、短期、正反馈、终身四条路径全面建模用户兴趣，终身路径支持最长10万个视频的层次化压缩

4. **会话级生成方式**：相比于传统的逐点预测，生成更优雅且上下文一致的推荐列表

5. **迭代偏好对齐（IPA）**：设计了Reward Model模拟用户反馈，结合ECPO策略实现稳定的强化学习优化

6. **格式正则化机制**：通过格式Reward解决RL引入的非法生成问题，避免挤压效应

7. **RL与SFT联合训练**：移除KL散度损失，SFT损失确保模型稳定性

---

## 11. OneRec-V2 版本改进

OneRec-V2在V1的基础上针对两大核心问题进行了改进：

### 11.1 Lazy Decoder-Only 架构

OneRec-V1的Encoder-Decoder架构存在一个严重的资源分配失衡问题——当上下文长度为512时，Encoder的序列编码消耗了97.66%的总计算FLOPs，而Decoder的目标生成仅占2.34%，这极大地限制了模型的可扩展性。OneRec-V2提出了一种**Lazy Decoder-Only架构**来彻底解决这一问题：

- **移除Encoder**：完全去除独立的Encoder模块，将用户上下文特征直接通过Context Processor处理为统一的key-value表示
- **Context Processor**：将异构的用户特征（用户画像、短期行为、长期行为等）拼接为统一的上下文序列，通过RMSNorm归一化后生成L_kv层共享的key-value对
- **Lazy Cross-Attention**：每个解码器层通过交叉注意力访问共享的key-value对，无需独立的K/V投影层。多个连续的decoder块共享同一套key-value表示，配合GQA（Grouped Query Attention）大幅减少内存占用
- **KV-Sharing**：进一步采用key-value统一表示（v_l = k_l），在保持性能的同时显著降低模型内存占用

这一架构使得**总计算量减少94%，训练资源降低90%**。在相同计算预算下，模型参数可以从0.5B扩展到8B（16倍于V1），且收敛损失高度符合Scaling Laws（E=3.13, A=3660, α=0.489）。

### 11.2 基于真实用户交互的偏好对齐

OneRec-V1的RL完全依赖Reward Model，面临两个固有缺陷：一是采样效率受限（仅能对1%用户做在线rollout），二是存在reward hacking风险（策略可能利用Reward Model的偏置模式而非真正的用户偏好）。OneRec-V2引入了**直接利用真实用户反馈信号**的强化学习框架：

**Duration-Aware Reward Shaping**：直接使用播放时长作为最密集的用户反馈信号，但播放时长天然受视频时长偏置影响。为此V2设计了时长感知奖励塑形机制——根据视频时长进行对数分桶，将目标视频的播放时长与同一时长桶内该用户历史视频的播放时长分布进行比较，计算其经验分位数作为归一化的参与度评分。这种方法有效消除了视频时长对奖励信号的偏置，使奖励准确反映内容质量而非视频长度。

**GBPO（Gradient-Bounded Policy Optimization）**：一种新提出的强化学习方法，解决了传统GRPO/PPO在负样本上的梯度不稳定问题。GBPO移除了传统的ratio clipping操作，改为对π_old施加动态约束——对于正样本，π_old' ≥ π_old；对于负样本，π_old' ≥ 1 - π_θ。其核心洞察是：BCE损失对负样本的梯度远比RL损失稳定（当前概率越小，抑制梯度越小），而RL损失恰好相反（概率越小，梯度越大容易爆炸）。GBPO通过用BCE损失的梯度来约束RL梯度，实现了**全样本利用**（不丢弃任何样本的梯度）与**梯度有界稳定化**的统一。

**Hybrid训练策略**：V2还对比了三种RL训练方式——纯Reward Model、纯用户反馈信号、以及两者结合的Hybrid模式。实验表明，Reward Model在互动类指标（点赞、关注、评论）上表现突出，而用户反馈信号在时长类指标上更优，两者结合的Hybrid方案能够兼顾多目标平衡，实现帕累托最优。

总结来说，OneRec-V2通过Lazy Decoder-Only架构大幅提升了模型可扩展性（支持8B参数），通过真实用户反馈驱动的RL框架实现了更精准的偏好对齐，在线A/B测试中快手主站App Stay Time提升0.467%，快手极速版提升0.741%。
