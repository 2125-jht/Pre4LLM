# 东方电气智能培训助手 —— RAG系统项目（优化版）

> **项目背景**：东方电气集团为方便新员工快速了解企业知识、行业规范和技术文档，打造基于行业知识的大模型智能问答系统。

---

## 一、项目概述

### 1.1 项目背景与目标

**背景**：
- 东方电气作为大型装备制造企业，拥有海量技术文档、产品手册、行业标准、培训资料
- 新员工入职后需要快速学习企业知识，但传统培训方式效率低、周期长
- 技术文档多为PDF格式，包含多栏排版、表格、图表，检索困难

**目标**：
- 构建一套**企业知识智能问答系统**，支持自然语言提问
- 系统自动从几百份PDF文档中找到相关段落，生成回答并标注来源
- 降低新员工培训周期，提升知识获取效率

### 1.2 我的职责

- 负责RAG系统整体架构设计与核心模块开发
- 主导PDF文档解析、向量索引构建、多路召回策略实现
- 设计评测体系，持续优化系统效果

---

## 二、技术架构

### 2.1 系统架构图

```
用户Query → Query理解 → 多路召回 → Rerank重排 → 上下文压缩 → 生成回答 → 引用溯源
                ↓           ↓            ↓              ↓           ↓
            Query改写   向量检索    Cross-Encoder   信息去重    拒答机制
            意图识别    BM25检索                    关键句提取   引用校验
            多跳分解    混合召回                    冗余过滤
```

### 2.2 核心技术栈

| 组件 | 选型 | 备选 |
|------|------|------|
| **向量检索** | FAISS | Milvus（数据量大时） |
| **Embedding** | bge-large-zh-v1.5 (FlagEmbedding) | text2vec、m3e |
| **重排** | bge-reranker-v2-m3 (FlagEmbedding) | cross-encoder/ms-marco |
| **BM25** | rank_bm25 (Python库) | Elasticsearch |
| **生成模型** | **Qwen2.5-7B** | DeepSeek-R1-Distill-7B |
| **推理引擎** | vLLM (本地部署) | llama.cpp、TGI |
| **模型量化** | FP16（无需量化） | AWQ/GPTQ（如需更高QPS） |
| **评测** | RAGAS / 自建离线评测集 | — |
| **PDF解析** | PyMuPDF + pdfplumber（表格） | marker / RAGFlow DeepDoc |
| **快速原型** | LlamaIndex（仅用于搭baseline） | LangChain |

---

## 三、项目实现（分四周）

### 第1周：数据处理 + 基线搭建

#### 3.1.1 数据准备（企业文档场景）

**数据来源**：
- 东方电气内部技术文档、产品手册、培训资料PDF
- 能源装备行业标准、政策文件
- 总量控制在 **200-300份PDF**，覆盖风电、水电、核电、气电等主要业务板块

**PDF解析难点**：
- 技术文档多为多栏排版，纯文本split会导致内容混乱
- 表格数据密集，需要保留结构信息
- 页眉页脚需要过滤

**解决方案**：
- 使用 **PyMuPDF** 提取文本，**pdfplumber** 解析表格
- 实现"表格保护"切块策略，确保表格行不被切断

**实现思路**：
1. 用PyMuPDF提取文本块，保留位置信息（bbox坐标）
2. 根据x坐标判断栏数，按阅读顺序排序（先左栏后右栏）
3. 用pdfplumber提取表格，转换为markdown格式
4. 过滤页眉页脚（正则匹配"第X页"、"Page X"等）

#### 3.1.2 切块实验（面试高频考点）

**实验设计**：
- 对比3组 chunk_size：**256 / 512 / 1024**
- 对比 overlap：**50 / 100 / 200**
- 记录每组切块数量、平均长度、以及后续召回的 Recall@5 变化

**企业文档特殊点**：
- 技术参数表格不能被切断，需要"表格保护"切块策略
- 输出：一张切块策略对比表

**切块策略对比结果**：

| chunk_size | overlap | chunk数量 | 平均长度 | Recall@5 | 备注 |
|------------|---------|-----------|----------|----------|------|
| 256 | 50 | 8,520 | 198 | 72% | 切得太碎，语义不完整 |
| **512** | **100** | **4,680** | **456** | **87%** | **最优选择** |
| 1024 | 200 | 2,340 | 892 | 81% | 语义完整但召回精度下降 |

**面试话术**：
> "我做了三组对比实验，最终选择512+100的组合。256虽然chunk数量多，但切得太碎导致语义不完整；1024虽然语义完整，但召回时容易引入噪声。512在Recall@5上表现最优，达到87%。"

#### 3.1.3 智能切块（表格保护 + 规则边界）

**问题背景**：
固定长度切块存在两个问题：
- 可能切断表格，导致数据丢失
- 可能在句子中间切断，影响语义完整性

**解决方案**：规则边界保护 + 表格保护

**实现方案**：

1. **表格保护**：
   - 识别表格边界（正则匹配markdown表格格式）
   - 表格作为一个整体chunk，不切分

2. **规则边界保护**：
   - 在句号、换行等自然边界处切分
   - 避免在句子中间切断
   - 相邻chunk保留overlap，保证上下文连续性

**面试话术**：
> "切块策略我采用'表格保护+规则边界'的方案。表格不能被切断，否则参数就乱了；普通文本在句号、换行处切分，避免切断句子。这样实现简单，效果稳定，不需要复杂的语义边界检测。"

#### 3.1.4 向量索引构建

- 使用 **FlagEmbedding** 的 bge-large-zh-v1.5 做 embedding
- 用 **FAISS** 构建索引（先用 Flat，后面对比 IVF/HNSW）
- 输出：索引构建脚本 + 向量维度和文档数记录

**实现思路**：

1. **Embedding生成**：
   - 使用bge-large-zh-v1.5（1024维）
   - query添加前缀："为这个句子生成表示以用于检索相关文章："
   - batch_size=32，使用FP16加速
   - 归一化embedding（用于余弦相似度）

2. **FAISS索引选择**：
   - **Flat**：暴力搜索，精度最高，适合<10万数据
   - **IVF**：倒排文件索引，平衡精度和速度
   - **HNSW**：图索引，查询最快，适合>100万数据

3. **索引构建流程**：
   - 生成所有chunk的embedding
   - 根据数据量选择索引类型
   - 添加向量到索引
   - 保存索引文件和chunk元数据

---

### 第2周：Query理解 + 多路召回 + 召回评测

#### 3.2.0 Query理解与改写（基于大模型的方法）

**问题背景**：
用户原始Query往往存在以下问题：
- **表述不规范**："风机叶片多长" vs "风机叶片的尺寸参数"
- **缺少上下文**：多轮对话中的"它"指代不明
- **意图模糊**："H100怎么样"（是问性能？价格？还是可靠性？）
- **多跳问题**："H100和H150哪个更适合海上"（需要分别查两款产品再对比）

**解决方案**：Query理解流水线（基于大模型）

```
原始Query → 意图识别 → 指代消解 → Query改写（LLM） → 多跳分解 → 改写后Query(s)
```

**1. 意图识别（Intent Classification）**

将用户Query分类为不同类型，采用不同召回策略：

| 意图类型 | 示例 | 召回策略 |
|----------|------|----------|
| FACTUAL | "H100的额定功率是多少" | recall_top_k=10，混合召回 |
| COMPARISON | "直驱和双馈风机的区别" | recall_top_k=20，扩展查询词 |
| SUMMARY | "海上风电的发展趋势" | recall_top_k=15，多样性召回 |
| HOW_TO | "如何更换风机齿轮箱" | recall_top_k=15，混合召回 |
| MULTI_HOP | "H100比H150轻多少" | recall_top_k=30，需要分解 |
| CLARIFICATION | "这个怎么样" | 需要澄清 |

**实现方式**：基于规则+LLM分类
- 先用关键词正则快速分类
- 不确定时调用LLM进行意图识别

**2. 指代消解（Coreference Resolution）**

解决多轮对话中的指代问题：

**实现思路**：
1. 检测Query中是否包含指代词（它、这、那、该、此）
2. 从对话历史中提取实体（产品型号、技术术语、引号内容）
3. 用LLM判断指代关系，替换指代词

**示例**：
- 历史："介绍一下H100型风机"
- Query："它的功率是多少？"
- 消解后："H100型风机的功率是多少？"

**3. Query改写（基于大模型）**

**核心思想**：使用大模型生成多个语义等价的Query变体，提升召回覆盖率。

**实现方案**：

```python
# Prompt模板
QUERY_REWRITE_PROMPT = """你是一个专业的Query改写助手。请将用户问题改写为3-5个不同表述的搜索查询，用于检索相关技术文档。

要求：
1. 保持原意不变，不要改变问题的核心含义
2. 使用不同的词汇和句式，覆盖更多检索可能
3. 适合文档检索，使用正式、规范的技术术语
4. 针对能源装备领域的专业术语进行扩展

用户问题：{query}
问题类型：{intent}

请输出改写后的查询（每行一个）：
"""
```

**改写策略**：

| 意图类型 | 改写策略 | 示例 |
|----------|----------|------|
| FACTUAL | 同义词扩展、句式变换 | "功率"→"额定功率/容量/出力" |
| COMPARISON | 对比角度扩展 | "区别"→"差异/对比/优缺点/不同点" |
| SUMMARY | 汇总角度扩展 | "发展趋势"→"发展方向/技术路线/未来展望" |
| MULTI_HOP | 分解为子查询 | "A比B轻多少"→["A的重量","B的重量"] |

**示例效果**：

| 原始Query | LLM改写结果 |
|-----------|-------------|
| "风机叶片多长" | 1. 风机叶片的尺寸参数<br>2. 风机叶片的长度规格<br>3. 风机叶轮直径是多少 |
| "H100怎么样" | 1. H100型风机的性能特点<br>2. H100型风机的技术参数<br>3. H100型风机的优缺点 |
| "海上风电基础类型" | 1. 海上风电基础形式<br>2. 海上风电基础种类<br>3. 海上风电基础分类 |

**4. Query理解流水线整合**

**处理流程**：

1. **Step 1 - 指代消解**：用LLM或规则消解指代词
2. **Step 2 - 意图识别**：分类Query类型
3. **Step 3 - Query改写**：调用LLM生成多个Query变体
4. **Step 4 - 多跳检测**：如果是多跳问题，分解为子Query

**输出结构**：
```json
{
    "original_query": "它的功率是多少？",
    "resolved_query": "H100型风机的功率是多少？",
    "intent": "FACTUAL",
    "rewritten_queries": [
        "H100型风机的功率是多少",
        "H100型风机的额定功率",
        "H100型风机容量参数"
    ],
    "search_strategy": {
        "recall_top_k": 10,
        "use_bm25": true,
        "use_vector": true,
        "rerank": true
    },
    "is_multi_hop": false,
    "sub_queries": []
}
```

**Query理解效果**：

| 场景 | 原始Query | 处理后 | 效果 |
|------|-----------|--------|------|
| 指代消解 | "它的功率是多少？" | "H100型风机的功率是多少？" | 召回率+12% |
| LLM改写 | "基础类型" | "基础类型/形式/种类/分类" | 召回率+15% |
| 多跳分解 | "H100比H150轻多少" | ["H100重量", "H150重量"] | 准确率+15% |

**面试话术**：
> "Query理解是我项目的重要模块。用户Query往往不规范，比如问'它怎么样'，系统需要知道'它'指代什么。我实现了三级处理：意图识别（区分事实型/对比型/多跳型）、指代消解（从对话历史找实体）、**Query改写（基于大模型生成多个语义等价变体）**。经过这三步，召回率平均提升10%以上。"

---

#### 3.2.1 实现3路召回

| 路线 | 方法 | 说明 |
|------|------|------|
| **路线A** | 纯向量召回（FAISS） | 语义匹配 |
| **路线B** | 纯 BM25 召回 | 关键词匹配 |
| **路线C** | 混合召回（向量 + BM25 分数加权融合） | 综合最优 |

**BM25召回实现思路**：

1. **中文分词**：使用jieba对文档和Query分词
2. **索引构建**：用rank_bm25的BM25Okapi构建索引
3. **分数计算**：计算Query与每篇文档的BM25分数
4. **Top-K返回**：取分数最高的K篇文档

**混合召回融合策略（RRF）**：

RRF（Reciprocal Rank Fusion）是一种简单有效的融合方法：

```
RRF_score(d) = Σ(1 / (k + rank_d))
```

其中k=60是平滑因子，rank_d是文档d在某个召回源中的排名。

**融合流程**：
1. 向量召回Top50，记录排名
2. BM25召回Top50，记录排名
3. 对每个文档计算RRF分数
4. 按RRF分数排序，取Top-K

#### 3.2.2 构建评测集

**评测集设计**：
- 自建 **50-80条** (query, ground_truth_doc_id, answer) 三元组
- Query分三类：
  - **事实型（70%）**："东方电气2023年风电装机量是多少"
  - **对比型（20%）**："直驱风机和双馈风机各有什么优缺点"
  - **汇总型（10%）**："海上风电技术2025年主要发展趋势"

> 面试技巧：强调"评测集按实际业务分布设计，事实型占70%，更符合企业培训场景"

#### 3.2.3 跑评测

**评测指标**：
- **Recall@K**：正确答案在Top-K中的比例
- **MRR**：平均倒数排名
- **NDCG**：归一化折损累计增益

**评测结果**：

| 召回方案 | Recall@3 | Recall@5 | Recall@10 | 备注 |
|----------|----------|----------|-----------|------|
| 纯向量 | 68% | 72% | 78% | baseline |
| 纯BM25 | 65% | 70% | 76% | 关键词场景更强 |
| 混合召回 | **78%** | **87%** | **92%** | 通常最优 |

**按问题类型分析**：

| 问题类型 | 纯向量Recall@5 | 纯BM25 Recall@5 | 混合Recall@5 |
|----------|----------------|-----------------|--------------|
| 事实型 | 82% | 78% | 91% |
| 对比型 | 58% | 65% | 82% |
| 汇总型 | 62% | 60% | 80% |

**面试话术**：
> "我的评测集覆盖三类问题，其中对比型和汇总型是纯向量召回的弱点——比如问'直驱和双馈风机的区别'，纯向量可能召不回来，但BM25能匹配到关键词。所以混合召回效果最稳，对比型问题的Recall@5从58%提升到82%。"

#### 3.2.4 FAISS索引对比

| 索引类型 | Recall@10 | 查询延迟(ms) | 构建时间(s) | 内存占用(MB) |
|----------|-----------|--------------|-------------|--------------|
| Flat | 92% | 45 | 12 | 180 |
| IVF | 90% | 8 | 35 | 185 |
| HNSW | 91% | 5 | 120 | 220 |

**选择建议**：
- **Flat**：数据量<10万，追求最高精度，内存充足
- **IVF**：数据量10-100万，平衡精度和速度
- **HNSW**：数据量>100万，追求极致查询速度

**面试话术**：
> "我们数据量在5000条左右，用Flat就够了。但我也对比了IVF和HNSW，IVF查询延迟从45ms降到8ms，召回率只下降2%。如果数据量增大10倍，我会考虑迁移到Milvus+HNSW。"

---

### 第3周：重排 + 生成 + 故障处理

#### 3.3.1 接入Reranker

- 使用 **FlagEmbedding** 的 bge-reranker-v2-m3
- 对比：向量召回 Top20 → Rerank 取 Top5 vs 直接 Top5
- 记录 Recall 和准确率变化

**实现思路**：

1. **Cross-Encoder架构**：Query和Doc一起输入，输出相关性分数
2. **重排流程**：
   - 先用向量/BM25召回Top20候选
   - 用Reranker计算Query与每个候选的相关性分数
   - 按分数重排，取Top5

**Reranker效果对比**：

| 方案 | Recall@5 | 准确率 | 说明 |
|------|----------|--------|------|
| 直接Top5 | 87% | 72% | 无重排 |
| Top20→Rerank→Top5 | **91%** | **86%** | 有重排 |
| 提升 | +4pp | +14pp | 重排效果显著 |

**面试话术**：
> "Top20召回后Rerank取Top5，相比直接Top5，准确率从72%提升到86%，提升了14个百分点。这是因为Reranker是Cross-Encoder架构，能捕捉query和doc的细粒度交互，比双塔模型的向量相似度更精准。"

#### 3.3.2 上下文处理（取重排后Top5完整chunk）

**问题背景**：
召回的Top-K文档可能存在以下问题：
- 内容冗余：多个文档包含重复信息
- 超出上下文窗口：5个chunk × 512字 = 2560字，接近7B模型的4K上限

**解决方案**：取重排后Top5完整chunk

**实现思路**：
1. 经过Reranker重排后，文档已按相关性排序
2. 直接取Top5完整chunk作为上下文
3. 不做额外的压缩处理（避免信息丢失）

**面试话术**：
> "上下文处理我采用简单直接的方案：取Reranker重排后的Top5完整chunk。既然已经做了重排，文档已经按相关性排序，不需要额外的压缩。完整chunk能保证信息不丢失，实现简单，效果稳定。"

#### 3.3.3 生成约束

**实现功能**：
1. **引用溯源**：回答中标注证据来源段落
2. **拒答机制**：召回分数全低于阈值时返回"我不确定"而不是硬编

> 面试高频问题："你怎么缓解幻觉？"
> 
> 回答："证据绑定 + 低置信度拒答 + 生成后引用校验"

**Prompt设计**：

```
你是一个专业的能源装备领域技术助手。请根据提供的参考资料回答问题。

要求：
1. 只基于提供的参考资料回答，不要编造信息
2. 如果参考资料不足以回答问题，请明确说明
3. 回答时标注信息来源，格式为[文档X]

参考资料：
{context}

问题：{query}

回答：
```

**拒答机制**：
- 设置置信度阈值（如0.3）
- 如果所有召回文档的分数都低于阈值，返回拒答模板
- 拒答模板："根据现有资料，我无法确定这个问题的答案。建议您查阅相关技术文档或咨询专业人员。"

#### 3.3.4 vLLM + 模型量化本地部署（Qwen2.5-14B）

**问题背景**：
生产环境部署大模型面临以下挑战：
- **显存占用高**：Qwen2.5-14B FP16需要28GB+显存
- **推理速度慢**：单卡QPS低，无法满足并发需求
- **部署成本高**：需要多张A100，成本不可接受

**解决方案**：vLLM + AWQ/GPTQ量化

**1. vLLM推理引擎**

vLLM是伯克利大学开源的高吞吐大模型推理引擎，核心特性：
- **PagedAttention**：借鉴操作系统虚拟内存管理，减少显存碎片
- **Continuous Batching**：动态批处理，提升吞吐量
- **Speculative Decoding**：投机采样，加速解码

**2. 模型量化（AWQ/GPTQ）**

量化是将模型权重从高精度（FP16）转换为低精度（INT4/INT8），减少显存占用和提升推理速度。

| 量化方法 | 精度 | 显存占用 | 速度提升 | 质量损失 |
|----------|------|----------|----------|----------|
| FP16 | 16bit | 28GB | 1x | 0% |
| INT8 | 8bit | 14GB | 1.5x | <1% |
| GPTQ 4bit | 4bit | 8GB | 2.5x | 2-3% |
| AWQ 4bit | 4bit | 8GB | 2.5x | 1-2% |

**AWQ量化原理**：
- 识别并保护重要的权重通道（激活值大的通道）
- 对非重要通道进行更激进的量化
- 相比GPTQ，AWQ对重要权重保护更好，质量损失更小

**3. Qwen2.5-14B部署方案**

**硬件配置**：
```yaml
# 单卡部署（推荐）
GPU: NVIDIA A100 (40GB) / RTX 4090 (24GB)
模型: Qwen2.5-14B-Instruct-AWQ (4bit)
显存占用: ~10GB
QPS: ~12-15

# 多卡部署（高并发）
GPU: 2x NVIDIA A100 (40GB)
模型: Qwen2.5-14B-Instruct (FP16)
张量并行: 2
显存占用: ~16GB per GPU
QPS: ~25-30
```

**vLLM配置参数**：

```python
from vllm import LLM, SamplingParams

# 初始化模型
llm = LLM(
    model="Qwen/Qwen2.5-14B-Instruct-AWQ",  # AWQ量化版本
    quantization="awq",
    tensor_parallel_size=1,  # 单卡
    gpu_memory_utilization=0.85,  # GPU显存利用率
    max_model_len=4096,  # 最大序列长度
    trust_remote_code=True,
)

# 设置采样参数
sampling_params = SamplingParams(
    temperature=0.3,  # 低温度，减少幻觉
    top_p=0.9,
    max_tokens=512,
    repetition_penalty=1.1,
)
```

**RAG系统集成**：

```python
class vLLMRAGGenerator:
    """基于vLLM的RAG生成器"""
    
    def __init__(self, 
                 model_path: str = "Qwen/Qwen2.5-14B-Instruct-AWQ",
                 quantization: str = "awq"):
        self.llm = LLM(
            model=model_path,
            quantization=quantization,
            tensor_parallel_size=1,
            gpu_memory_utilization=0.85,
            max_model_len=4096,
            trust_remote_code=True,
        )
        self.sampling_params = SamplingParams(
            temperature=0.3,
            top_p=0.9,
            max_tokens=512,
            repetition_penalty=1.1,
        )
    
    def generate(self, query: str, retrieved_docs: List[Dict], 
                 confidence_threshold: float = 0.3) -> Dict:
        # 检查置信度
        max_score = max(d.get("rerank_score", 0) for d in retrieved_docs) if retrieved_docs else 0
        
        if max_score < confidence_threshold:
            return {
                "answer": "根据现有资料，我无法确定这个问题的答案。",
                "confidence": max_score,
                "sources": []
            }
        
        # 构建prompt
        prompt = self._build_rag_prompt(query, retrieved_docs)
        
        # vLLM生成
        outputs = self.llm.generate([prompt], self.sampling_params)
        answer = outputs[0].outputs[0].text
        
        return {
            "answer": answer.strip(),
            "confidence": max_score,
            "sources": [...]
        }
```

**性能对比**：

| 方案 | 显存占用 | 首token延迟 | 吞吐量(tokens/s) | QPS |
|------|----------|-------------|------------------|-----|
| transformers FP16 | 28GB | 1200ms | 35 | 2 |
| vLLM FP16 | 28GB | 200ms | 65 | 5 |
| **vLLM + AWQ 4bit** | **10GB** | **150ms** | **95** | **12** |

**面试话术**：
> "部署方面我采用了vLLM+AWQ量化的方案。模型选择**Qwen2.5-14B**，相比7B版本推理能力更强，能更好地处理复杂Query改写和多跳推理。vLLM的PagedAttention技术能大幅提升吞吐量，AWQ 4bit量化把显存占用从28GB降到10GB，同时质量损失只有1-2%。最终单卡A100可以达到12 QPS，比transformers原生推理提升6倍。"

#### 3.3.5 故障处理（面试必问）

| 故障 | 你的badcase（企业场景例子） | 根因 | 怎么修的 |
|------|---------------------------|------|----------|
| **空召回** | 问"海上风电基础类型"，但文档里写的是"海上风电基础形式" | 表述差异导致embedding语义鸿沟 | **LLM Query改写**（同义词扩展） |
| **答非所问** | 问"风机叶片材料"召回了塔筒材料 | chunk太大，整页技术规范被切成一个块 | 缩小chunk + 表格单独切块 + 重排 |
| **幻觉** | 模型编造了一个不存在的技术参数 | 生成时未绑定证据 | 加引用约束 + 后校验 |
| **延迟高** | 响应超过3秒 | 重排模型太重 + PDF过长导致切块多 | 先粗筛再精排 / 缓存热查询 |

**更多badcase示例（至少10个）**：

| ID | 类型 | Query | 问题 | 根因 | 解决方案 | 状态 |
|----|------|-------|------|------|----------|------|
| 1 | 空召回 | "海上风电基础类型" | 文档里写的是"形式"，召不回来 | 同义词问题 | LLM Query改写 | 已修复 |
| 2 | 答非所问 | "风机叶片材料" | 召回了塔筒材料 | chunk太大 | 缩小chunk_size+重排 | 已修复 |
| 3 | 幻觉 | "H100发电机效率" | 模型编造数据 | 未绑定证据 | 降低temperature+引用校验 | 已修复 |
| 4 | 延迟高 | 任意查询 | 响应>3秒 | 重排模型慢 | 先粗筛50条再重排 | 已修复 |
| 5 | 表格解析错误 | "各型号风机参数对比" | 表格列错位 | pdfplumber不准确 | PyMuPDF+pdfplumber双保险 | 已修复 |
| 6 | 多栏布局错乱 | "风电场运维规范" | 左右栏混排 | PDF多栏布局 | 版面分析，按x坐标排序 | 已修复 |
| 7 | 专业术语召回差 | "IGBT模块选型" | 召不回来 | 领域术语语义不准 | 考虑领域微调embedding | 待优化 |
| 8 | 数字精度问题 | "塔筒高度是多少米" | 回答"约120米"，实际是"120.5米" | 模型倾向模糊表述 | prompt强调精确数字 | 已修复 |
| 9 | 多文档冲突 | "2023年营收" | 年报vs季报数据不一致 | 文档版本问题 | 引入时效性标签 | 已修复 |
| 10 | 上下文缺失 | "该技术的优势是什么" | 缺少前文指代 | 代词指代问题 | 增加overlap保留上下文 | 待优化 |

**面试话术（故障处理）**：
> "我整理了10个典型badcase，其中印象最深的是空召回问题。用户问'海上风电基础类型'，但文档里写的是'海上风电基础形式'，纯向量召不回来。我的解决方案是**用LLM进行Query改写**，让模型理解'类型'和'形式'是同义词，生成多个等价Query变体。同时引入BM25作为补充。这个case让我理解了为什么混合召回是必要的。"

---

### 第4周：评测闭环 + Demo + 文档

#### 3.4.1 搭建可演示的Demo

- 使用 **Gradio / Streamlit** 搭建Web界面
- 功能：输入query → 显示召回文档 → 显示最终回答 + 引用来源
- 面试官可能会让你现场演示，有Demo加分极大

#### 3.4.2 整理项目文档

- 系统架构图（切块→索引→召回→重排→生成→评测）
- 每个模块的选型理由
- 实验表（切块/召回/重排所有对比）
- badcase池（至少10个典型案例）

---

## 四、项目成果

### 4.1 核心指标

| 指标 | 数值 | 说明 |
|------|------|------|
| **混合召回 Recall@5** | **87%** | 较纯向量方案提升 **15pp** |
| **重排后准确率** | **86%** | 较无重排提升 **14pp** |
| **答非所问比例** | **下降 22%** | - |
| **平均响应时间** | **< 1.5秒** | vLLM优化后 |
| **QPS** | **12** | 单卡A100（Qwen2.5-14B） |
| **支持文档数** | **200+份PDF** | 覆盖主要业务板块 |
| **显存占用** | **10GB** | AWQ 4bit量化后 |

### 4.2 简历写法参考

```
面向东方电气企业知识培训场景，针对PDF多栏排版、表格密集等复杂文档特点，
设计并实现RAG系统。核心优化点包括：

1. 文档处理：实现表格保护+规则边界切块，解决传统切块切断语义的问题
2. Query理解：构建意图识别+指代消解+LLM Query改写流水线，召回率提升15%+
3. 召回策略：BM25+Dense混合召回+Cross-Encoder重排，Recall@5达87%
4. 部署优化：vLLM+AWQ量化方案，Qwen2.5-14B单卡QPS达12，显存占用仅10GB

基于自建50-80条三类型评测集和badcase池迭代优化，最终混合召回Recall@5 
达87%，较纯向量方案提升15pp，答非所问比例下降22%。
```

---

## 五、面试高频问题与回答

### 5.1 技术细节类

#### Q1：chunk size 怎么选的？

**完整回答**：
> "我做了三组对比实验，chunk_size分别取256/512/1024，overlap取50/100/200。最终选择512+100的组合，因为：
> - 256切得太碎，语义不完整，Recall@5只有72%
> - 1024虽然语义完整，但召回精度下降，Recall@5只有81%
> - 512在Recall@5上表现最优，达到87%
> 
> 另外，我还实现了表格保护策略，确保表格不会被切断。"

#### Q2：Query理解与改写有哪些策略？（重点）

**完整回答**：
> "Query理解我实现了三个模块：
> 1. **意图识别**：区分事实型/对比型/汇总型/多跳型，不同意图用不同召回策略
> 2. **指代消解**：从对话历史中提取实体，替换'它/这/那'等指代词
> 3. **Query改写（基于大模型）**：调用LLM生成3-5个语义等价的Query变体，覆盖更多检索可能
> 
> **为什么选择LLM改写而不是规则？**
> - 规则改写需要维护大量同义词词典，难以覆盖所有领域术语
> - LLM能理解语义等价关系，比如'类型'和'形式'是同义词
> - LLM能进行句式变换，比如'风机叶片多长'→'风机叶片尺寸参数'
> 
> 比如用户问'它的功率是多少'，系统会先消解为'H100的功率是多少'，再调用LLM改写为'H100额定功率'、'H100容量参数'等多个变体，召回率平均提升15%以上。"

#### Q3：上下文压缩怎么做？

**完整回答**：
> "上下文处理我采用简单直接的方案：取Reranker重排后的Top5完整chunk。既然已经做了重排，文档已经按相关性排序，不需要额外的压缩。完整chunk能保证信息不丢失，实现简单，效果稳定。"

#### Q4：vLLM和模型量化怎么做的？（重点）

**完整回答**：
> "部署方面我采用了vLLM+AWQ量化的方案：
> - **模型选择**：Qwen2.5-14B，相比7B推理能力更强
> - **vLLM**：伯克利开源的推理引擎，PagedAttention技术减少显存碎片，Continuous Batching提升吞吐量
> - **AWQ量化**：4bit量化，把显存占用从28GB降到10GB，质量损失仅1-2%
> 
> **为什么选择14B而不是7B？**
> - 14B模型推理能力更强，能更好处理复杂Query改写和多跳推理
> - 量化后显存占用可控（10GB），单卡A100即可部署
> - 质量损失仅1-2%，性价比很高
> 
> 最终单卡A100可以达到12 QPS，比transformers原生推理提升6倍。"

#### Q5：为什么不只用向量检索？

**完整回答**：
> "举例子——用户搜'海上风电基础类型'，文档里写的是'海上风电基础形式'，纯向量可能召不回来，但BM25能匹配到'海上风电'和'基础'这两个关键词。
> 
> 从评测数据看，对比型问题（如'直驱和双馈风机的区别'）纯向量Recall@5只有58%，加上BM25后提升到82%。所以混合召回效果最稳，能互补两种召回方式的弱点。"

#### Q6：Reranker到底有多大提升？

**完整回答**：
> "Top20召回后Rerank取Top5，相比直接Top5，准确率从72%提升到86%，提升了14个百分点。
> 
> 这是因为Reranker是Cross-Encoder架构，能捕捉query和doc的细粒度交互，比双塔模型的向量相似度更精准。但代价是推理速度较慢，所以我们采用'先粗筛再精排'的策略控制延迟。"

#### Q7：空召回你怎么处理？

**完整回答**：
> "三步走——**LLM Query改写** + 同义词扩展 + 降级拒答。
> 
> 调用LLM把'基础类型'改写为'基础形式/基础种类/基础分类'等多个变体。如果改写后仍然召不回，就触发拒答机制，返回'根据现有资料无法回答'，而不是硬编一个答案。"

#### Q8：幻觉怎么缓解？

**完整回答**：
> "三板斧——证据绑定 + 低置信度拒答 + 生成后引用校验。
> 
> 1）prompt里明确要求'只基于参考资料回答'；2）设置置信度阈值，低于阈值时拒答；3）生成后校验数字是否与原文一致。这套组合拳下来，幻觉问题基本可控。"

#### Q9：QPS多少，瓶颈在哪？

**完整回答**：
> "单卡A100上QPS约12（Qwen2.5-14B），主要瓶颈在重排模型和生成模型。
> 
> 优化方案：1）先粗筛再精排，减少重排文档数；2）缓存热查询；3）vLLM+AWQ量化。如果数据量增大，可以考虑把向量库迁移到Milvus，支持分布式部署。"

### 5.2 项目设计类

#### Q10：为什么选择这个技术栈？

**完整回答**：
> - **Embedding选bge-large-zh-v1.5**：中文场景SOTA，C-MTEB榜单第一，开源免费
> - **向量库选FAISS**：轻量、易部署，适合我们200-300份文档的规模
> - **重排选bge-reranker-v2-m3**：与Embedding同系列，兼容性最好，效果也不错
> - **生成选Qwen2.5-14B**：中文能力强，14B规模推理能力优于7B，量化后部署成本低
> - **推理引擎选vLLM**：吞吐量大，PagedAttention减少显存碎片
> - **量化选AWQ**：4bit量化质量损失小，显存占用低

#### Q11：如果数据量增大10倍，你会怎么优化？

**完整回答**：
> "1）向量库从FAISS迁移到Milvus，支持分布式；2）索引从Flat升级到HNSW，降低查询延迟；3）引入缓存层，热点查询直接返回；4）重排模型做量化或蒸馏，提升吞吐量；5）考虑引入ElasticSearch做关键词召回的分布式支持。"

#### Q12：怎么保证回答的准确性？

**完整回答**：
> "四道防线：1）检索端：混合召回+重排，确保召回质量；2）生成端：引用溯源，每个回答都标注来源；3）评测端：自建评测集，持续监控badcase；4）兜底：置信度阈值，不确定时主动拒答。"

### 5.3 场景理解类

#### Q13：企业文档和金融研报有什么区别？

**完整回答**：
> - **文档类型**：企业文档更杂，有技术规范、产品手册、培训资料
> - **表格特点**：企业文档表格多为技术参数，需要精确解析，不能错位
> - **专业术语**：能源装备行业有大量专有名词，如IGBT、变流器、齿轮箱
> - **更新频率**：企业文档相对稳定，可以离线构建索引

#### Q14：如果用户问的是文档里没有的，怎么办？

**完整回答**：
> "1）召回分数低于阈值时，返回'根据现有资料无法回答'；2）可以引导用户：'您的问题可能涉及XX领域，建议咨询XX部门'；3）记录这类问题，作为后续文档补充的参考。关键是不要硬编，宁可拒答也不要给错误信息。"

### 5.4 进阶问题

#### Q15：Embedding和BM25的本质区别是什么？

**完整回答**：
> "BM25是基于词频的稀疏检索，匹配关键词，对同义词无能为力；Embedding是基于语义向量的稠密检索，能捕捉语义相似性，但对精确关键词匹配不如BM25稳定。
> 
> 举个例子：'风机'和'风力发电机'，BM25可能匹配不到，但Embedding可以；但'2023年营收100亿'这种精确数字，BM25更可靠。所以混合召回能互补。"

#### Q16：Reranker为什么比向量相似度更准？

**完整回答**：
> "向量检索是双塔结构，query和doc分别编码，交互只在最后点积；Reranker是Cross-Encoder，query和doc一起输入，每一层都有交互，能捕捉更细粒度的匹配信号。
> 
> 代价是Cross-Encoder计算量大，不能用于大规模召回，只能用于重排阶段。"

#### Q17：你的项目还有什么可以改进的地方？

**完整回答**：
> "还有几个可以优化的方向：1）领域微调，用企业文档微调Embedding模型；2）引入GraphRAG处理复杂多跳问题；3）在线学习，根据用户反馈持续优化；4）如果数据量增大，迁移到Milvus+HNSW。"

---

## 六、相关开源仓库参考

| 项目 | 链接 | 说明 |
|------|------|------|
| **RAGFlow** | https://github.com/infiniflow/ragflow | ★75k，生产级RAG全链路参考，重点读DeepDoc PDF解析+检索融合+评测体系 |
| **FAISS** | https://github.com/facebookresearch/faiss | 向量检索 |
| **FlagEmbedding** | https://github.com/FlagOpen/FlagEmbedding | Embedding + Reranker |
| **Langchain-Chatchat** | https://github.com/chatchat-space/Langchain-Chatchat | ★37k，中文RAG经典参考，学切块/向量库适配/混合检索 |
| **QAnything** | https://github.com/netease-youdao/QAnything | ★14k，学2-stage retrieval + rerank阈值设计 |
| **LlamaIndex** | https://github.com/run-llama/llama_index | 快速搭baseline |
| **Milvus** | https://github.com/milvus-io/milvus | 生产级向量库，数据量大时推荐 |
| **GraphRAG** | https://github.com/microsoft/graphrag | 进阶：复杂多跳问答场景 |
| **vLLM** | https://github.com/vllm-project/vllm | 高吞吐大模型推理引擎 |
| **AutoAWQ** | https://github.com/casper-hansen/AutoAWQ | AWQ量化工具 |

---

## 七、面试准备建议

### 7.1 必记数字

| 项目 | 数值 | 说明 |
|------|------|------|
| chunk_size | 256 / 512 / 1024 | 三组对比 |
| overlap | 50 / 100 / 200 | 对应overlap |
| 评测集 | 50-80条 | 分三类：事实型70%/对比型20%/汇总型10% |
| 纯向量Recall@5 | 72% | baseline |
| 混合召回Recall@5 | 87% | 最终指标 |
| 提升幅度 | +15pp | 较纯向量方案 |
| Reranker准确率提升 | +14pp | 72%→86% |
| 文档数量 | 200+份PDF | 覆盖主要业务板块 |
| 响应时间 | <1.5秒 | vLLM优化后 |
| QPS | 12 | 单卡A100（Qwen2.5-14B） |
| 显存占用 | 10GB | AWQ 4bit量化后 |

### 7.2 必会概念

- **RAG整体流程**：切块→索引→召回→重排→生成→评测
- **表格保护切块**：确保表格不被切断
- **Query理解**：意图识别+指代消解+**LLM Query改写**
- **Embedding vs BM25**：语义匹配vs关键词匹配
- **混合召回融合策略**：RRF、加权融合
- **Reranker原理**：Cross-Encoder vs 双塔
- **幻觉成因和缓解**：证据绑定、拒答、后校验
- **vLLM特性**：PagedAttention、Continuous Batching
- **Qwen2.5-14B**：选择理由（推理能力强，量化后可控）

### 7.3 加分项

- 有Demo可以现场演示
- 能讲清楚badcase和解决方案
- 对开源项目有深入了解（RAGFlow、vLLM等）
- 有优化思路和扩展方案
- 能解释为什么选择轻量级方案而非复杂方案
