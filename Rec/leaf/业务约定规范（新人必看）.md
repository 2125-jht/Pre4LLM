# 代码提交
提交不需要再带上 dynamic_json_config.json

git add .
不会自动加上 dynamic_json_config.json

想要提交到分支上测试：
git add -f dynamic_json_config.json

测试完发 CR 之前：
git rm dynamic_json_config.json
git commit

# 召回
约定俗成的 reason 格式应该是：
内流非首屏是3位数字
内流首屏是5位数字
外流是4位数字
